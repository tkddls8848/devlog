{
  "name": "news",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "eleventy --serve --incremental",
    "build": "eleventy",
    "digest": "node tools/news-digest.mjs",
    "digest:dry": "node tools/news-digest.mjs --dry-run",
    "feeds:check": "node tools/feed-check.mjs",
    "deploy": "PATH_PREFIX=/ eleventy && npx --yes wrangler@4 deploy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  },
  "engines": {
    "node": ">=20"
  }
}
